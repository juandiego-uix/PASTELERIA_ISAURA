const tokenKey="isaura-admin-token";const $=s=>document.querySelector(s);let dashboard;function toast(message){const item=$("#toast");item.textContent=message;item.classList.add("show");setTimeout(()=>item.classList.remove("show"),2500)}async function api(path,options={}){const response=await fetch(path,{headers:{...(options.body instanceof FormData?{}:{"Content-Type":"application/json"}),Authorization:`Bearer ${localStorage.getItem(tokenKey)||""}`,...(options.headers||{})},...options});if(response.status===204)return null;const body=await response.json().catch(()=>({}));if(!response.ok)throw new Error(body.error||"Solicitud no válida");return body}function showAdmin(){$("#login-view").hidden=true;$("#admin-view").hidden=false;loadDashboard()}function render(){const metrics=dashboard.metrics;$("#metrics").innerHTML=`<div class="metric"><span>Pedidos pendientes hoy</span><strong>${metrics.pending_today}</strong></div><div class="metric"><span>Entregados este mes</span><strong>${metrics.delivered_month}</strong></div><div class="metric"><span>Productos publicados</span><strong>${dashboard.products.length}</strong></div>`;$("#orders-table").innerHTML=dashboard.orders.map(order=>`<tr><td><strong>${order.fecha}</strong><br>${order.hora}</td><td><strong>${escapeHtml(order.nombre_cliente)}</strong><br><small>${escapeHtml(order.contacto)}</small></td><td>${escapeHtml(order.descripcion)}</td><td>$${Number(order.precio).toLocaleString("es-CO")}<br><small>${escapeHtml(order.tipo_pago)}</small></td><td><select class="status-select" data-status="${order.id}">${["Pendiente","En Preparación","Entregado"].map(status=>`<option ${status===order.estado?"selected":""}>${status}</option>`).join("")}</select></td><td><button class="danger" data-delete-order="${order.id}">Eliminar</button></td></tr>`).join("")||`<tr><td colspan="6">No hay pedidos todavía.</td></tr>`;$("#products-list").innerHTML=dashboard.products.map(product=>`<article class="admin-product"><img src="${product.image_url||`/uploads/${encodeURIComponent(product.imagen||"")}`}" alt=""><strong>${escapeHtml(product.nombre)}</strong><small>${escapeHtml(product.categoria)}</small><button class="danger" data-delete-product="${product.id}">Eliminar</button></article>`).join("")||"<p>No hay productos.</p>"}function escapeHtml(value){return String(value).replace(/[&<>'"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","'":"&#039;",'"':"&quot;"}[c]))}async function loadDashboard(){try{dashboard=await api("/api/admin/dashboard");render()}catch(error){toast(error.message)}}$("#login-form").addEventListener("submit",async event=>{event.preventDefault();const data=Object.fromEntries(new FormData(event.currentTarget));try{const result=await api("/api/auth/login",{method:"POST",body:JSON.stringify(data)});localStorage.setItem(tokenKey,result.token);showAdmin()}catch(error){$("#login-error").textContent=error.message}});if(localStorage.getItem(tokenKey))showAdmin();$("#logout").addEventListener("click",()=>{localStorage.removeItem(tokenKey);location.reload()});document.querySelectorAll(".tab").forEach(tab=>tab.addEventListener("click",()=>{document.querySelectorAll(".tab").forEach(item=>item.classList.remove("active"));tab.classList.add("active");$("#orders-panel").hidden=tab.dataset.tab!=="orders";$("#products-panel").hidden=tab.dataset.tab!=="products"}));$("#new-order").addEventListener("click",()=>$("#order-dialog").showModal());$("#new-product").addEventListener("click",()=>$("#product-dialog").showModal());$("#order-form").addEventListener("submit",async event=>{event.preventDefault();try{await api("/api/admin/orders",{method:"POST",body:JSON.stringify(Object.fromEntries(new FormData(event.currentTarget)))});event.currentTarget.closest("dialog").close();event.currentTarget.reset();await loadDashboard();toast("Pedido guardado")}catch(error){toast(error.message)}});$("#product-form").addEventListener("submit",async event=>{event.preventDefault();try{await api("/api/admin/products",{method:"POST",body:new FormData(event.currentTarget)});event.currentTarget.closest("dialog").close();event.currentTarget.reset();await loadDashboard();toast("Producto guardado")}catch(error){toast(error.message)}});$("#orders-table").addEventListener("change",async event=>{if(!event.target.dataset.status)return;try{await api(`/api/admin/orders/${event.target.dataset.status}`,{method:"PATCH",body:JSON.stringify({estado:event.target.value})});toast("Estado actualizado")}catch(error){toast(error.message)}});document.addEventListener("click",async event=>{const order=event.target.dataset.deleteOrder;const product=event.target.dataset.deleteProduct;if(!order&&!product)return;if(!confirm("¿Eliminar este registro?"))return;try{await api(`/api/admin/${order?"orders":"products"}/${order||product}`,{method:"DELETE"});await loadDashboard();toast("Registro eliminado")}catch(error){toast(error.message)}});
