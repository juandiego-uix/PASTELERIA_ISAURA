const state={products:[],favorites:JSON.parse(localStorage.getItem("isaura-favorites")||"[]")};
const $=selector=>document.querySelector(selector);
const imageUrl=product=>product.image_url||(`/uploads/${encodeURIComponent(product.imagen||"")}`);
function toast(message){const element=$("#toast");element.textContent=message;element.classList.add("show");setTimeout(()=>element.classList.remove("show"),2800)}
async function api(path,options={}){const response=await fetch(path,{headers:{"Content-Type":"application/json",...(options.headers||{})},...options});const body=await response.json().catch(()=>({}));if(!response.ok)throw new Error(body.error||"No se pudo completar la solicitud");return body}
function renderProducts(){const query=$("#search").value.toLowerCase().trim();const products=state.products.filter(product=>`${product.nombre} ${product.categoria}`.toLowerCase().includes(query));$("#catalog-status").textContent=products.length?`${products.length} ${products.length===1?"creación disponible":"creaciones disponibles"}`:"No encontramos resultados con esa búsqueda.";$("#products").innerHTML=products.map((product,index)=>`<article class="product-card" style="animation-delay:${index*50}ms"><div class="product-image" data-image="${imageUrl(product)}"><img loading="lazy" src="${imageUrl(product)}" alt="${escapeHtml(product.nombre)}"></div><div class="product-info"><div><p class="product-category">${escapeHtml(product.categoria)}</p><h3>${escapeHtml(product.nombre)}</h3></div><button class="like ${state.favorites.includes(product.nombre)?"active":""}" data-favorite="${escapeHtml(product.nombre)}" aria-label="Añadir ${escapeHtml(product.nombre)} a favoritos">♥</button></div></article>`).join("")}
function escapeHtml(value){return String(value).replace(/[&<>'"]/g,char=>({"&":"&amp;","<":"&lt;",">":"&gt;","'":"&#039;",'"':"&quot;"}[char]))}
function renderFavorites(){const list=$("#favorite-list");$("#favorite-count").textContent=state.favorites.length;$("#fab-count").textContent=state.favorites.length;list.innerHTML=state.favorites.length?state.favorites.map((name,index)=>`<div class="favorite-row"><span>${escapeHtml(name)}</span><button class="remove" data-remove="${index}">Quitar</button></div>`).join(""):"<p class='status'>Aún no has elegido ningún postre.</p>";localStorage.setItem("isaura-favorites",JSON.stringify(state.favorites));renderProducts()}
function openFavorites(open){$("#favorites-panel").classList.toggle("open",open);$("#favorites-panel").setAttribute("aria-hidden",String(!open));$("#scrim").hidden=!open}
async function loadProducts(){try{const response=await api("/api/products");state.products=response.data||[];renderProducts();renderFavorites()}catch(error){$("#catalog-status").textContent="No pudimos cargar el catálogo. Revisa la configuración de Supabase.";toast(error.message)}}
$("#search").addEventListener("input",renderProducts);$("#clear-search").addEventListener("click",()=>{$("#search").value="";renderProducts()});$("#open-favorites").addEventListener("click",()=>openFavorites(true));$("#close-favorites").addEventListener("click",()=>openFavorites(false));$("#scrim").addEventListener("click",()=>openFavorites(false));$("#products").addEventListener("click",event=>{const favorite=event.target.closest("[data-favorite]");if(favorite){const name=favorite.dataset.favorite;state.favorites=state.favorites.includes(name)?state.favorites.filter(item=>item!==name):[...state.favorites,name];renderFavorites()}const image=event.target.closest("[data-image]");if(image)window.open(image.dataset.image,"_blank","noopener")});$("#favorite-list").addEventListener("click",event=>{const remove=event.target.closest("[data-remove]");if(remove){state.favorites.splice(Number(remove.dataset.remove),1);renderFavorites()}});$("#send-order").addEventListener("click",async()=>{if(!state.favorites.length){toast("Selecciona al menos un postre");return}const description=`Selección desde la web:\n${state.favorites.map(name=>`- ${name}`).join("\n")}`;try{await api("/api/orders",{method:"POST",body:JSON.stringify({nombre_cliente:"Cliente web",contacto:"WhatsApp",fecha:new Date().toISOString().slice(0,10),hora:"12:00",descripcion,tipo_pago:"Pendiente de Pago",precio:0,abono:0})});}catch(error){console.warn("No se registró el pedido:",error.message)}window.open(`https://wa.me/573215457378?text=${encodeURIComponent(`Hola Isaura, me interesan estos postres:\n${state.favorites.map(name=>`- ${name}`).join("\n")}`)}`,"_blank","noopener")});loadProducts();
